# Docker Environment Configuration for MCP WhatsApp Web
# Copy this file to .env and adjust as needed

# Logging level: fatal, error, warn, info, debug, trace
LOG_LEVEL=info

# Transport type: stdio, sse, or http (streamable HTTP)
TRANSPORT=http

# SSE port (if using SSE transport)
SSE_PORT=3001

# FFmpeg path (already installed in container)
FFMPEG_PATH=/usr/bin/ffmpeg

# Optional: WhatsApp session directory (mounted volume)
# SESSION_DIR=/app/whatsapp-sessions

# Optional: Media storage directory (mounted volume)
# MEDIA_DIR=/app/media

# Optional: Public base URL for media links (returned by download_media)
# MEDIA_PUBLIC_BASE_URL=http://192.168.10.32:3003

# Optional: Settings file path (persisted runtime settings)
# SETTINGS_PATH=/app/media/settings.json

# Optional: Upload settings (also configurable via admin page)
# UPLOAD_MAX_MB=50
# UPLOAD_ENABLED=true
# UPLOAD_MAX_FILES=1
# REQUIRE_UPLOAD_TOKEN=false
# UPLOAD_TOKEN=change-me

# Optional: Auto-download media (also configurable via admin page)
# AUTO_DOWNLOAD_MEDIA=false
# AUTO_DOWNLOAD_MAX_MB=50
